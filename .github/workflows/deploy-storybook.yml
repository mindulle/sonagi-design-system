name: Deploy Storybook

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    name: Build and Deploy to Cloudflare Pages
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
      pull-requests: write # PR에 Preview URL 코멘트를 남기기 위해 필요

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Tokens
        run: pnpm --filter @mindulle/tokens build

      - name: Build Storybook
        run: pnpm --filter @mindulle/ui build-storybook

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: 'sonagi-design-system' # 프로젝트 이름이 다를 경우 수정 필요 (예: design-sonagi-space)
          directory: 'packages/ui/storybook-static'
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
